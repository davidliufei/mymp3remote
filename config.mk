export BUILD_ROOT = $(shell pwd)
export HEAD_PATH  = $(BUILD_ROOT)/inc
BUILD_DIR = $(BUILD_ROOT)/lcd/ \
            $(BUILD_ROOT)/usb/ \
            $(BUILD_ROOT)/media \
            $(BUILD_ROOT)/math \
            $(BUILD_ROOT)/app
            
